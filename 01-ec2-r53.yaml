- name: create ec2 and r53
  hosts: local
  connection: local
  vars:
    ami-id: AKIA3JLOY5Z4DQ7CQ2FY
    sg-id: s+Gng8u34eaV3060RIvgDxSxVkYgZDV+DHFqV3oQ
    zone-id: Z0476340LOT78SX6IPUE
    domain-name: "hhrp.life"
    instances:
    - mongodb
  tasks:
  - name: create ec2 instance
    amazon.aws.ec2_instance:
      name: "{{ item }}"
      instance_type: t3.micro
      security_group: "{{ sg_id }}"
      image_id: "{{ ami_id }}"
      tags:
        Name: "{{ item }}"
    loop: "{{ instances }}"
    register: ec2_output

  - name: print the ec2_output
    ansible.builtin.debug:
      msg: "{{ ec2_output }}"       
